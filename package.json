{
  "name": "pashu-setu",
  "private": true,
  "version": "1.0.0",
  "description": "Pashu-Setu — field-to-action livestock health surveillance prototype",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && npm install && npm start",
    "test": "cd server && npm test"
  },
  "engines": { "node": ">=22.5.0" }
}
